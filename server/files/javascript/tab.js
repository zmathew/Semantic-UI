semantic.dropdown = {};

// ready event
semantic.dropdown.ready = function() {

  // selector cache
  var
    // alias
    handler
  ;

  // event handlers
  handler = {

  };

};


// attach ready event
$(document)
  .ready(semantic.dropdown.ready)
;